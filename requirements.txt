# Telegram
python-telegram-bot==21.6

# LLM providers (install only what you need based on LLM_PROVIDER in .env)
groq>=0.9.0                # for LLM_PROVIDER=groq  ← FREE, recommended
# anthropic>=0.34.0        # for LLM_PROVIDER=anthropic (paid)
# openai>=1.40.0           # for LLM_PROVIDER=openai (paid)
# google-generativeai>=0.7 # for LLM_PROVIDER=gemini (free tier)

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4
python-multipart==0.0.9

# Database
sqlalchemy==2.0.35

# Scheduler
apscheduler==3.10.4

# HTTP client (file proxy)
httpx==0.27.0

# Config
python-dotenv==1.0.1

# Tests
pytest==8.3.2
